#include "049.cpp"
//#include "buildTree.h"

int main()
{
    Solution so;
//    int a[] = {0,1,0,2,1,0,1,3,2,1,2,1};
//    vector<int> v(a, a + 12);
   // sort(v.begin(),v.end());

    string a[] = {"eat", "tea", "tan", "ate", "nat", "bat"};
    vector<string> v(a,a+6);
    so.groupAnagrams(v);

}
